{
  "dependencies": {
    "autoprefixer": "^10.4.27",
    "pagefind": "^1.3.0",
    "postcss": "^8.5.8",
    "postcss-cli": "^11.0.1",
    "postcss-import": "^16.1.1"
  },
  "scripts": {
    "index": "pagefind --site public --output-path static/pagefind",
    "build": "hugo --configDir config-texify3 && pagefind --site public"
  }
}
